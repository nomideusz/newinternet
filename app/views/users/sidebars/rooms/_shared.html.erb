<%= link_to_room room,
      id: "room_#{room.id}_list", data: { sorted_list_name: room.name },
      style: "--column-gap: 0.5em", class: [ "align-center gap room btn txt-nowrap", "unread": local_assigns[:unread] ] do %>
  <span class="overflow-ellipsis"><%= room.name %></span>
<% end %>
